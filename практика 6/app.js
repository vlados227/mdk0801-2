// let square = () => {
//     let number = prompt("введите число");
//     alert(`ваше число: ${number * number}`);
// }
// square();

let summ = () => {
    let first, second;
    Number(first, second);
    first = prompt("number 1: ");
    second = prompt("number 2: ");
    return first+second;
}
document.write(summ());